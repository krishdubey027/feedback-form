import React from 'react'
import FeedbackForm from './pages/FeedbackForm'

/**
 * App.jsx
 * Root application wrapper.
 * Renders the FeedbackForm page — single-page application with no routing.
 * Backend developers: Add your AuthProvider, ThemeProvider, or Router wrapper here.
 */
export default function App() {
  return <FeedbackForm />
}
